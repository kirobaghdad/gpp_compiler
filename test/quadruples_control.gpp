int helper(int x, int y = 2) {
    int sum = x + y;

    if (sum > 10) {
        sum -= 1;
    } else {
        sum += 2;
    }

    do {
        sum--;
    } while (sum > x);

    return sum;
}

int main() {
    int result = helper(5);
    return result;
}
